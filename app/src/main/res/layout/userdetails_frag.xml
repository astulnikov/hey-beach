<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/imgProfileCover"
        android:layout_width="match_parent"
        android:layout_height="@dimen/userdetails_cover_image_height"
        android:src="@drawable/placeholder"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/txtUserEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/dummy_email"
        android:textSize="@dimen/userdetails_user_email_text"
        android:textColor="@color/colorUserDetailsEmail"
        app:layout_constraintTop_toBottomOf="@id/imgProfileCover"
        android:padding="@dimen/element_default_margin"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</android.support.constraint.ConstraintLayout>